import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignContent: 'center',
  },
  canvasButton: {
    flex: 1,
  },
  canvasProgress: {
    position: 'absolute',
    bottom: 0,
    right: 0,
  },
});
